import * as React from "react";
import { StyleSheet, View } from "react-native";

const MdicheckboxMarkedCircle = () => {
  return <View style={styles.mdicheckboxMarkedCircle} />;
};

const styles = StyleSheet.create({
  mdicheckboxMarkedCircle: {
    flex: 1,
    width: "100%",
    height: 30,
    overflow: "hidden",
  },
});

export default MdicheckboxMarkedCircle;
